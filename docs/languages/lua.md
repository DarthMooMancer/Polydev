## Keybinds
```lua
-- These are the defaults for lua
require("Polydev").lua.setup({
    keybinds = {
        ["<Esc>"] = "CloseTerminal",
        ["<leader>pr"] = "LuaRun",
        ["<leader>nf"] = "NewLuaFile",
    }
})
```
